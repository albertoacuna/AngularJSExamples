'use strict';

describe('myApp.inputType module', function() {

  beforeEach(module('myApp.inputType'));

  describe('inputType controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});